import React from 'react';
import '../../style/components/circularProgress.scss';

const CircularProgress = () => (
  <div className="circularProgress">
    <span className="circularProgress__loader" />
  </div>
);

export default CircularProgress;
